<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rong.mapper.ReviewMapper">
    <insert id="insert">
        insert into review(id,userId,freePath,payPath)
        values(#{id},#{userId},#{freePath},#{payPath})
    </insert>
</mapper>